This directory is reserved for the default CharStreamer model bundle.

Release builds may vendor a validated bundle into `models/default/` before
building the wheel. The bundle files are generated artifacts and are ignored by
git unless intentionally added through a model-release process.
